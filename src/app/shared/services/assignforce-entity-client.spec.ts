import { AssignforceEntityClient } from './assignforce-entity-client';

describe('AssignforceEntityClient', () => {
  it('should create an instance', () => {
    expect(new AssignforceEntityClient()).toBeTruthy();
  });
});
